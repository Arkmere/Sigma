import type { SigmaService } from '../../domain/service.js';
import type { Profile } from '../../domain/model.js';
import type { RecordMode } from './records.js';
import { field, today } from './html.js';
export function saveProfile(service: SigmaService, data: FormData, editingId: string): void { const input = { displayName: field(data, 'displayName'), profileType: field(data, 'profileType') as Profile['profileType'], relationshipLabel: field(data, 'relationshipLabel'), dateOfBirth: field(data, 'dateOfBirth'), notes: field(data, 'notes') }; editingId ? service.updateProfile(editingId, input) : service.createProfile(input); }
export function saveRecord(service: SigmaService, mode: RecordMode, data: FormData): void { const profileId = field(data, 'profileId'); const recordedAt = new Date().toISOString(); const category = field(data, 'category'); if (mode === 'measurement') { const value = Number(field(data, 'value')); const unit = field(data, 'unit'); service.addMeasurement({ profileId, category, measurementType: field(data, 'measurementType'), label: field(data, 'label'), value, unit, originalValue: value, originalUnit: unit, measuredAt: field(data, 'measuredAt'), recordedAt, sourceType: 'manual', sourceName: field(data, 'sourceName') || undefined, acquisitionMethod: 'manual', notes: field(data, 'notes') || undefined }); } else if (mode === 'standard_size') service.addStandardSize({ profileId, category, label: field(data, 'label'), sizingSystem: field(data, 'sizingSystem'), sizeValue: field(data, 'sizeValue'), recordedAt, sourceType: 'manual', notes: field(data, 'notes') || undefined }); else service.addBrandFit({ profileId, category, brand: field(data, 'brand'), productName: field(data, 'productName') || undefined, productLine: field(data, 'productLine') || undefined, sizingSystem: field(data, 'sizingSystem'), sizeValue: field(data, 'sizeValue'), fitNotes: field(data, 'fitNotes') || undefined, recordedAt, sourceType: 'manual' }); }
export function addHistory(service: SigmaService, id: string, data: FormData): void { const value = Number(field(data, 'value')); const unit = field(data, 'unit'); service.addMeasurementValue(id, { value, unit, originalValue: value, originalUnit: unit, measuredAt: field(data, 'measuredAt'), recordedAt: new Date().toISOString(), sourceType: 'manual', acquisitionMethod: 'manual', notes: field(data, 'notes') || undefined }); }
export function downloadBackup(service: SigmaService): void { const blob = new Blob([JSON.stringify(service.exportBackup(), null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = `sigma-backup-${today()}.json`; link.click(); URL.revokeObjectURL(url); }
